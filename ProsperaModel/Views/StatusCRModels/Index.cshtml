@model IEnumerable<StatusCRModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DescStatusCR)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CRModel)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DescStatusCR)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CRModel)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdStatusCR">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdStatusCR">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdStatusCR">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
