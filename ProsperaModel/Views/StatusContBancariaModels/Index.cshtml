@model IEnumerable<StatusContBancariaModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DescStatusContBan)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ContBancariaModel)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DescStatusContBan)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ContBancariaModel)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdStatusContBan">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdStatusContBan">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdStatusContBan">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
