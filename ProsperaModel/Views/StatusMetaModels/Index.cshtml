@model IEnumerable<StatusMetaModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DescStatusMeta)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MetaModel)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DescStatusMeta)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MetaModel.IdMeta)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdStatusMeta">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdStatusMeta">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdStatusMeta">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
