@model IEnumerable<StatusTransferenciaModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DescStatusTransfe)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TransferenciaModel)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DescStatusTransfe)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TransferenciaModel.IdTransferencia)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdStatusTransfe">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdStatusTransfe">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdStatusTransfe">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
