@model IEnumerable<StatusOrcamentoModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DescStatusOrca)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OrcamentoModel)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DescStatusOrca)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OrcamentoModel.IdOrcamento)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdStatusOrca">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdStatusOrca">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdStatusOrca">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
